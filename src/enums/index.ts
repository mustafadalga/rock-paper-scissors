export enum GameChoice {
    Rock = "rock",
    Paper = "paper",
    Scissors = "scissors",
}

export enum GameOutcome {
    TIE,
    WIN,
    LOSS
}

export enum GameState {
    Pending,
    Started,
    Finished
}
